---
const { img, name, site } = Astro.props;
---
<style>
    .member {
        width: 200px;
        padding: 5px;
        margin: 20px;
        display: flex;
        border-radius: 15px;
        background-color: #078969;
        align-items: center;
    }
    .text {
        margin: 5px;
    }
    img {
        border-radius: 360%;
        width: 50px;
        height: 50px;
        margin: 3px;
    }
    h1 {
        color: #fafed7;
    }
    p {
        color: white;

    }
</style>
<a class="member" href={`https://${site}`}>
    <img src={`https://${img}`}>
    <div class="text">
        <h1>{name}</h1>
    </div>
</a>